<script type="text/javascript">
$(function() {
  $('#subscription').ajaxForm({
    url : '<%= '/' / @page.handle / 'subscription' %>',
    success : tb_remove
  });

  $('#subscription a.cancel').click(function() {
    tb_remove();
    return false;
  });
});
</script>



<p>Notify me by email whenever someone updated <q><%= @page.title %></q>.</p>

<%= form(:action => '/' / @page.handle / 'subscription', :method => 'post', :id => 'subscription') do %>

  <label for="email">Email Address</label>
  <%= text_field :id => 'email', :name => 'email' %>
  <%= submit 'Subscribe' %>
  <a href="#" class="cancel">Cancel</a>
<% end =%>

<p class="help">Don't worry: every notification email will have an unsubscribe link.</p>
